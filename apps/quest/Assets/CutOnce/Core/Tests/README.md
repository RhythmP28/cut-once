# Core/Tests

`Fixtures/` here is a copy of `data/fixtures`, made by `pnpm sync:fixtures` (every `pnpm quest:*` command runs it
first). It is gitignored: change `data/fixtures`, never the copy. EditMode tests read it from disk.
